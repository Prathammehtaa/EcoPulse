FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir prometheus_client>=0.19.0

COPY Model_Pipeline/src/metrics_exporter.py /app/metrics_exporter.py

ENV REPORTS_DIR=/app/reports
ENV METRICS_PORT=8000

EXPOSE 8000

CMD ["python", "metrics_exporter.py", "--serve"]
